export enum Direction {
    Top = 'Top',
    Bottom = 'Bottom',
    Left = 'Left',
    Right = 'Right'
};

// 旋转偏移量
export enum RotateOffset {
    Top = 0,
    Bottom = 180,
    Left = 270,
    Right = 90
}
